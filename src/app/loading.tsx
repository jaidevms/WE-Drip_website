export default function Loading() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-brand-off-white" aria-busy="true">
      <p className="font-mono text-xs uppercase tracking-wide text-brand-gray-text">Loading WeDrip…</p>
    </main>
  );
}
